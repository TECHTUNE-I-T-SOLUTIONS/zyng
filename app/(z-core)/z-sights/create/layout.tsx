import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Create Z-Sight | Zyng',
  description: 'Share a project, innovation, portfolio item, or creative work with the Zyng community.',
};

export default function CreateSightLayout({ children }: { children: React.ReactNode }) {
  return children;
}
