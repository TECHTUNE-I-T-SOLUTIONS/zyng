import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Join Zyng | Create Your Account',
  description: 'Create your Zyng account and join your verified school, campus, or alumni community.',
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
