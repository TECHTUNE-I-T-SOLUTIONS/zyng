see this documentation:
D:\Codes\zyng\public\documentations.md


now, let's start work on the full UI now, create all the pages inside the (z-core) folder, all the pages needed in the z-core pages should be there, then, we can start on the backend api, oh, we're not fully relying on api routes, they'll be very think, the core logics will be in the lib folders, so, now, create all the z-core pages needed, then, give me a very comprehensive and fully detailed schema, what I want is, all the tables needed, the user's tables where all their details will be saved, phone number, email, full actual name, z_name (means username but personalised; we'll limit it to one persona per person), interests, department, faculty, university, the list of faculties and departments are in the folder "D:\Codes\zyng\public\schools", cos we're considering future scaling to other universities as well, if you can get the data from online sources, make sure they're correct, fetch them and create the respective json files for them in the folder as well so we can use it, the logic will be to have them in json files and in the database (and see this suggestion: Use slug as unique key (NOT name)
Add:
faculty_id
department_id
school_id (for multi-school scaling)
and Consider adding:
"is_active": true, as we add schools and when we're ready to activate their onboarding as well), so, instead of fetching them from the database all the time, we'll use the json files, but when saving, we'll use the database ones. then, we'll collect other details from the students, for the platform to be fully personalised, and very friendly. then, we'll have other tables, for schools, faculties, departments, posts, also, make sure the posts table have all the post types based on the type of post that the user created, check the documentation i pasted, then, likes, comments, shares, we'll implement messaging features as well, we'll use supabase realtime and other features., events tables, 